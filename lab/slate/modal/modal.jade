.overview
  .card-space
  .card-actions
    button(ng-click="modal.close()") Close
.content(ng-class='{"ready" : modal._frame._ready}')
  iframe.frame(ng-if="modal._show",src="{{modal._frame._src}}", onload='iframeLoaded()')
  .loader
     .fa.fa-circle-o-notch.fa-spin
