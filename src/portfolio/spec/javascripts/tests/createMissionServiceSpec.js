describe('Missions', function () {
  var service,
      $httpBackend;

  beforeEach(function() {
    module('slate');

  });

  it("Should run a test", function () {
    expect(1).toEqual(1);
  });
});