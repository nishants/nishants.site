describe('Scroller', function () {

  describe('Loading', function () {

    it("should load module", function () {
      expect(window.Scroller).not.toBeNull();
    });

    it("should load default state", function () {
      expect(window.Scroller).not.toBeNull();
    });

  });

});