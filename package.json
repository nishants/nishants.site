{
  "name": "zinc",
  "private": true,
  "version": "0.0.0",
  "description": "A prototype for Zinc.",
  "repository": "https://github.com/nishants/zinc-oxide",
  "license": "WTFPL",
  "devDependencies": {
    "bower": "^1.7.7",
    "browserify": "^14.4.0",
    "eslint": "^3.14.1",
    "http-server": "^0.9.0",
    "jasmine-core": "^2.4.1",
    "karma": "^0.13.22",
    "karma-browserify": "^5.1.1",
    "karma-chrome-launcher": "^0.2.3",
    "karma-firefox-launcher": "^0.1.7",
    "karma-jasmine": "^0.3.8",
    "karma-junit-reporter": "^0.4.1",
    "uglify-js": "^2.7.5",
    "watch": "^1.0.1",
    "watch-run": "^1.2.5",
    "watchify": "^3.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "lint": "eslint src; exit 0",
    "scss": "scss src/index.scss:public/index.css",
    "scss-dev": "watch 'scss --watch src/index.scss:public/index.css' src",
    "jade": "jade --pretty src/index.jade -o public",
    "jade-dev": "jade --pretty src/index.jade -o public",
    "js-dev": "browserify src/index.js -o public/index.js",
    "js": "browserify src/index.js | uglifyjs > public/index.js",
    "copy-public": "rm -rf public/assets && cp -r src/app/assets public/assets",
    "copy-templates": "jade --pretty -H src/app/assets/templates -o public/assets/templates",
    "package": " npm run test-once && npm run js && npm run lint && npm run scss && npm run jade && npm run copy-public && npm run copy-templates",
    "compile": " npm run js-dev && npm run scss && npm run jade-dev && npm run copy-public && npm run copy-templates",
    "test": "karma start karma.conf.js",
    "test-once": "karma start karma.conf.js --single-run",
    "start": "http-server -a localhost -p 8000 -c-1 ./public",
    "watch": "watch-run -p 'src/**' npm run package"
  }
}
